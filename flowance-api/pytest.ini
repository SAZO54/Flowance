[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = apps
addopts =
    --strict-markers
    --ds=config.settings.test
markers =
    domain: DBを使わない業務ルールの検証
    application: ユースケース単位の検証
    integration: PostgreSQL / Redis / Celeryを伴う検証
